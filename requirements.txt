smolagents[transformers]==1.20.0   # agent framework + HF backend
torch>=2.2.0                       # Apple-silicon wheel on PyPI
transformers>=4.53.0               # HF models
accelerate>=1.8.0                  # device utils for HF
streamlit>=1.34.0                  # web UI
llama-cpp-python>=0.2.61           # optional GGUF backend (CPU/Metal)
transformers>=4.53.0
accelerate>=1.8.0
eptr2>=0.1.0                       # MCP solver wrapper  (replace with real version)
# -----------------------------------------------------------------
# DO NOT list gurobipy here – install it separately from pypi.gurobi.com# 0) be in the project root
# rm -rf .venv                       # drop old env (optional but safer)
# python3 -m venv .venv
# source .venv/bin/activate
# pip install --upgrade pip
# pip install -r requirements.txt    # installs Streamlit, etc.
# pip install --extra-index-url https://pypi.gurobi.com gurobipy==12.0.2